# Open Relic — agent guide

An API-only starting point for an open-source, self-hostable Git service built on Cloudflare Durable Objects. See [README.md](./README.md) for the architecture and development commands.

## Agent skills

### Issue tracker

Issues live as GitHub issues in `kcc989/open-relic`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — one `CONTEXT.md` and one `docs/adr/` at the repo root. See `docs/agents/domain.md`.
